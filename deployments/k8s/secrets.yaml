apiVersion: v1
kind: Secret
metadata:
  name: llmcp-secrets
type: Opaque
data:
  # TODO: store base64-encoded secrets via external secret management in real deployments.
  # POSTGRES_PASSWORD: <base64>
  # API_KEYS: <base64>

